# include <stdio.h>
# include <math.h>

int main() {
    float r;
    printf("O valor do raio do circulo e de: ");
    scanf("%f",&r);
    printf("O valor da area do circulo e de %.2f", M_PI * (r * r));
    return 0;
}